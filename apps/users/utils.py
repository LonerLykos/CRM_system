def changing_password(user, request_type: str):
    activation_url = ''

    # if request_type == 'activation':
    #     activation_token = generate_activation_token(user)
    #     activation_url = f"{settings.front}set-password/{activation_token}"
    # elif request_type == 'set-password':
    #
    # return
    pass